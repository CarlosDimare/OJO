kind = "web"
previewPath = "/"
title = "Web Terminal"
version = "1.0.0"
id = "artifacts/web-terminal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22843

[services.development]
run = "pnpm --filter @workspace/web-terminal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/web-terminal", "run", "build" ]
publicDir = "artifacts/web-terminal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22843"
BASE_PATH = "/"
